GLEW header should be in a folder named "GL" and the file should be named "glew.h"

GLFW3 header should be in a folder named "GLFW" and the file should be named "glfw3.h"

stb_image header should be in a folder named "stb" and the file should be named "stb_image.h"
